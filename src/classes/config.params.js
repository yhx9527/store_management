const date = new Date()
export default {
    outputPageSize: 20,
    year: date.getFullYear(),
    month: date.getMonth() + 1,
    day: date.getDate(),
    producePageSize: 20,
    productPageSize:10,
    userPageSize:10,
}